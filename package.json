{
  "name": "electron-superapp",
  "version": "1.0.0",
  "description": "A modular superapp built with Electron and SQLite",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"npm run dev:renderer\" \"wait-on http://localhost:3000 && electron .\"",
    "dev:renderer": "vite --mode development",
    "dev:punky": "concurrently \"npm run dev:renderer:punky\" \"wait-on http://localhost:3000 && electron .\"",
    "dev:renderer:punky": "vite --mode punky",
    "build": "vite build && electron-builder",
    "build:punky": "vite build --mode punky && electron-builder",
    "build:renderer": "vite build",
    "build:renderer:punky": "vite build --mode punky",
    "postinstall": "electron-builder install-app-deps"
  },
  "keywords": [
    "electron",
    "superapp",
    "sqlite",
    "miniapps"
  ],
  "author": "SuperApp Developer",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^28.0.0",
    "electron-builder": "^24.6.4",
    "sass": "^1.92.0",
    "vite": "^5.0.0",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "electron-store": "^8.1.0",
    "sqlite3": "^5.1.6",
    "crypto-js": "^4.2.0"
  },
  "build": {
    "appId": "com.superapp.electron",
    "productName": "SuperApp",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/main/**/*",
      "dist-renderer/**/*",
      "node_modules/**/*"
    ]
  }
}
