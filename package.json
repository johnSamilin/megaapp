{
  "name": "electron-superapp",
  "version": "1.0.0",
  "description": "A modular superapp built with Electron and SQLite",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"npm run dev:renderer\" \"wait-on http://localhost:3000 && electron .\"",
    "dev:renderer": "node -e \"require('./esbuild.config.js').serve()\"",
    "dev:punky": "concurrently \"npm run dev:renderer:punky\" \"wait-on http://localhost:3000 && electron .\"",
    "dev:renderer:punky": "THEME=punky node -e \"require('./esbuild.config.js').serve()\"",
    "build": "npm run build:renderer && electron-builder",
    "build:punky": "npm run build:renderer:punky && electron-builder",
    "build:renderer": "node -e \"require('./esbuild.config.js').build()\"",
    "build:renderer:punky": "THEME=punky node -e \"require('./esbuild.config.js').build()\"",
    "postinstall": "electron-builder install-app-deps"
  },
  "keywords": [
    "electron",
    "superapp",
    "sqlite",
    "miniapps"
  ],
  "author": "SuperApp Developer",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^28.0.0",
    "electron-builder": "^24.6.4",
    "esbuild": "^0.25.10",
    "sass": "^1.93.2",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "electron-store": "^8.1.0",
    "roughjs": "^4.6.6",
    "sqlite3": "^5.1.6"
  },
  "build": {
    "appId": "com.superapp.electron",
    "productName": "SuperApp",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/main/**/*",
      "dist-renderer/**/*",
      "node_modules/**/*"
    ]
  }
}
